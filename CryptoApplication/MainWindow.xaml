<Window x:Class="CryptoApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoApplication" 
        xmlns:views="clr-namespace:CryptoApplication.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <views:SearchBarView x:Name="SearchBar" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="10"/>
        <views:CurrencyListView x:Name="CurrencyList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,60,10,10"/>
    </Grid>
</Window>
